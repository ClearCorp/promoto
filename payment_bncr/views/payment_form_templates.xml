<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="payment_bncr.method_form" inherit_id="payment.method_form">
        <input name="o_payment_radio" position="attributes">
            <attribute name="t-if" t-elif="provider_code == 'bncr'">
                t-att-data-bncr-inline-form-values="provider_sudo._bncr_get_inline_form_values()"
            </attribute>
        </input>
    </template>

    <template id="payment_submit_button_inherit" inherit_id="payment.submit_button">
        <button name="o_payment_submit_button" position="before">
            <div
                id="o_bncr_button_container" name="o_payment_submit_button" data-is-bncr="true"
            >
                <div id="o_bncr_enabled_button" class="d-none"/>
                <div id="o_bncr_disabled_button"/>
            </div>
            <div id="o_bncr_loading" class="d-flex justify-content-center d-none">
                <div class="spinner-border"/>
            </div>
        </button>
    </template>

</odoo>